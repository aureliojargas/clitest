$ cd /tmp
$ url="https://github.com/txt2tags/old/raw/master/txt2tags-2.6.tgz"
$ curl -fsSOL "$url"                       # download
$ du -h txt2tags-2.6.tgz                   # verify size
532K	txt2tags-2.6.tgz
$ tar tzf txt2tags-2.6.tgz | sed -n '$='   # verify number of files
545
$ tar xzf txt2tags-2.6.tgz                 # extract
$ cd txt2tags-2.6
$ ls -1F | LC_ALL=C sort                   # list contents
COPYING
ChangeLog
README
doc/
extras/
po/
samples/
test/
txt2tags*
$ bin_dir=/tmp/bin
$ test -d $bin_dir || mkdir $bin_dir       # create bin dir if needed
$ cp -v txt2tags $bin_dir                  # install to bin dir
'txt2tags' -> '/tmp/bin/txt2tags'
$
